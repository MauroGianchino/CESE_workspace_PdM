Punto 2: se crean las variables necesarias dentro de la funcion main (no son globales). Se da inicio al delay y dentro del while(1) se chequea con un IF si se cumple el tiempo del delay. Si lo hace, se togglea el led.

Punto 3: Aca lo que hice fue crear una funcion llamada "sequence" la cual tiene como argumentos la cantidad de parpadeos del led, el periodo (en milisegundos) y el duty cicle (de 0 a 100). Entonces se define un array con todos los periodos que uno quiera y dentro del while(1) se itera con esta funcion, representando cada patron deseado.


Preguntas:

-¿Se pueden cambiar los tiempos de encendido de cada led fácilmente en un solo lugar del código o éstos están hardcodeados?

En mi caso si, se modifican en el arreglo creado dentro de la funcion main.


-¿Qué bibliotecas estándar se debieron agregar para que el código compile? Si las funcionalidades crecieran, habría que pensar cuál sería el mejor lugar para incluir esas bibliotecas y algunos typedefs que se usan en el ejercicio.


Hubo que agregar stdint.h y bool.h. El mejor lugar para incluirlas es en el main.h.


-¿Es adecuado el control de los parámetros pasados por el usuario que se hace en las funciones implementadas? ¿Se controla que sean valores válidos? ¿Se controla que estén dentro de los rangos correctos?

En mi caso en las tres funciones del punto 2 si se revisan. Se podrian hacer mas chequeos seguramente, como el valor de la variable duration. En el caso de la funcion sequence no realizo ningun chequeo, por lo que se deberian agregar tambien.


-¿Cuán reutilizable es el código implementado? 

Creo que las 3 funciones del punto 2 y la funcion sequence son bastante reutilizables, Logicamente seria ideal de usarla en codigos mas generales y no solo por dos o tres secuencias particulares.


-¿Cuán sencillo resulta en su implementación cambiar el patrón de tiempos de parpadeo?

Al crear la funcion "sequence", considero que es muy sencillo cambiar el patron y, ademas, agregar nuevos patrones.


